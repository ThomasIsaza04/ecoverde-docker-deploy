apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-ecoverde
spec:
  replicas: 2
  selector:
    matchLabels:
      app: web-ecoverde
  template:
    metadata:
      labels:
        app: web-ecoverde
    spec:
      containers:
        - name: nginx
          image: nginx:alpine
          ports:
            - containerPort: 80
